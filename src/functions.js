
// Here we can create a functions file for all functions and make .collections into a variable for use all across by making this function a compenent. 

// firebase
// .firestore()
// .collection('times')
// .add({
//     title,
//     time_hours: parseInt(time) //The parseInt() function parses a string argument and returns an integer of the specified radix (the base in mathematical numeral systems ie. Binary, Decimal etc. ).
// }).then(() => {
//     setTime('')
//     setTitle('')
// })


